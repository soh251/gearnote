{
  "name": "gearnote",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && pagefind --site dist && node scripts/check-bold.mjs",
    "check:bold": "node scripts/check-bold.mjs",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.3.0",
    "astro": "^6.4.3",
    "tailwindcss": "^4.3.0"
  },
  "devDependencies": {
    "pagefind": "^1.5.2"
  }
}
